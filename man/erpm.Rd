% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/erpm.R
\name{erpm}
\alias{erpm}
\title{ERPM main wrapper: translate and optionally fit with \pkg{ergm}}
\usage{
erpm(
  formula,
  eval.call = TRUE,
  verbose = FALSE,
  estimate = NULL,
  eval.loglik = NULL,
  control = NULL,
  timeout = NULL,
  seed = NULL,
  nodes = NULL,
  dyads = list(),
  group_labels = NULL,
  constraints = NULL,
  mh_moves = NULL,
  mh_weights = NULL
)
}
\arguments{
\item{formula}{ERGM/ERPM formula. LHS must evaluate either to a partition
vector or to a bipartite \pkg{network} object.}

\item{eval.call}{Logical. If \code{FALSE}, return the constructed
\code{ergm()} call instead of evaluating it.}

\item{verbose}{Logical verbosity flag.}

\item{estimate}{Optional \pkg{ergm} estimation mode.}

\item{eval.loglik}{Passed to \code{ergm()} when non-NULL.}

\item{control}{Optional \code{control.ergm} object or list forwarded to
\code{ergm::control.ergm()}.}

\item{timeout}{Optional timeout in seconds for evaluation.}

\item{seed}{Optional RNG seed.}

\item{nodes}{Optional node table or named list coercible to a node table when
the LHS is a partition vector.}

\item{dyads}{Optional named list of dyadic matrices; for backward compatibility,
a single matrix is also accepted when the RHS contains exactly one dyadic term.}

\item{group_labels}{Optional group labels passed to
\code{build_bipartite_from_inputs()}.}

\item{constraints}{Optional constraints formula. Defaults to \code{~ b1part}.}

\item{mh_moves}{Optional ERPM MH move names forwarded to \code{ErpmMix}.}

\item{mh_weights}{Optional ERPM MH move weights forwarded to \code{ErpmMix}.}
}
\value{
Either the evaluated \code{ergm()} result or the unevaluated call
when \code{eval.call = FALSE}.
}
\description{
ERPM main wrapper: translate and optionally fit with \pkg{ergm}
}
\examples{
\dontrun{
  partition <- c(1, 1, 2, 2, 3)
  fit <- erpm(partition ~ groups(2) + cliques(k = 2), eval.call = FALSE)
}

}
\keyword{ERGM}
\keyword{ERPM}
\keyword{bipartite}
\keyword{wrapper}
