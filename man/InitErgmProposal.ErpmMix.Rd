% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/InitErgmProposal.ErpmMix.R
\name{InitErgmProposal.ErpmMix}
\alias{InitErgmProposal.ErpmMix}
\alias{ErpmMix}
\title{ERGM proposal: ErpmMix}
\usage{
InitErgmProposal.ErpmMix(arguments, nw)
}
\arguments{
\item{arguments}{A list of proposal arguments (may be empty).}

\item{nw}{A \pkg{network} object (unused here; required by ergm API).}
}
\value{
A list describing the compiled proposal for \pkg{ergm}.
}
\description{
ErpmMix is a Metropolis-Hastings proposal that draws one partition move type
at each MCMC iteration and then applies the corresponding ERPM step.

Supported moves:
\itemize{
\item "toggle" : ErpmToggleStep (2 toggles)
\item "swap"   : ErpmSwapStep   (4 toggles)
\item "merge"  : ErpmMergeStep  (merge one non-empty group into another)
\item "split"  : ErpmSplitStep  (split a non-empty group toward an empty one)
}
}
\details{
User-facing arguments:
\itemize{
\item moves   : character vector in \{"toggle","swap","merge","split"\}
\item weights : positive numeric vector of same length as moves
}

Important ergm note:
\itemize{
\item When this function is called through ergm's \code{.select("ErpmMix")}, the object
passed as \code{arguments} usually contains meta-entries such as \code{constraints}
and \code{reference}, while the user payload from \code{MCMC.prop.args[[i]]} is stored
in the first unnamed element: \code{arguments[[1]]}.
}

Decoding policy:
\itemize{
\item If user args are missing/empty, use the default move set
moves=c("toggle","swap","merge","split") with weights=c(2,1,0,0).
\item If \code{moves} is missing/empty, use defaults.
\item If \code{moves} contains unknown entries, do not error: fall back to the canonical
default mix returned to C, namely toggle:2 and swap:1.
\item If \code{moves} contains duplicates, treat it as invalid input and fall back to the
same canonical default mix.
\item If \code{weights} is missing, use default weights when \code{moves} is exactly the default
move set; otherwise use a vector of 1s aligned with the selected moves.
\item If \code{weights} is invalid (non-numeric, wrong length, non-finite, <=0), fall back
to the canonical default mix.
}

Packing convention to C:
\itemize{
\item iinputs = c(K, move_codes...)
\item inputs  = c(weights...)
where move codes are stable integers:
toggle = 1, swap = 2, merge = 3, split = 4
}

Notes:
\itemize{
\item This R initializer only packs the user-requested move mixture.
\item Feasibility is handled later by the C proposal code. In particular, the move
draw itself stays state-independent; if a selected move is impossible in the
current network state, the C backend falls back to TOGGLE.
\item As a consequence, observed move frequencies along the chain do not have to
match the nominal user weights exactly, especially when merge/split are often
infeasible on the visited states.
}
}
\author{
Jérémie Chichignoud - Cub'itech
}
